# RYUL — 2주차 진행상황 보고

**팀명:** HANTONE  
**공모전:** 문화체육관광부 AI·데이터 활용 공모전  
**앱명:** RYUL (율)  
**보고일:** 2026-06-13  
**담당:** AI / 백엔드 개발

---

## 1. 1주차 대비 변경 사항 요약

| 항목 | 1주차 | 2주차 |
|------|-------|-------|
| 훈련 데이터 | AIHub 290개 (장단/조 혼합) | 국악원 API 306개 + AIHub 악기별 분리 468개 |
| AI 모델 종류 | 리듬 모델 16개 (장구용) | 리듬 16개 + **음고 모델 25개** (가야금·대금) |
| 세그먼트 수 | 649개 | **888개** (국악원 API 데이터 재학습) |
| 악기별 AI | 장구(반주)만 가능 | **가야금·대금·장구 3악기 모두 AI 생성 가능** |
| 데이터 수집 | AIHub ZIP 수동 추출 | **국립국악원 공공API 자동 다운로드 구현** |

---

## 2. 2주차 완료 내역

### 2-1. 국립국악원 공공데이터 API 연동

국립국악원 국악디지털음원 공공데이터 API를 연동해 훈련 데이터를 자동으로 수집합니다.

```
공공데이터포털 API (XML)
  serviceKey, rhythm, tranditional_key 파라미터
    ↓
  <wav_file_path> 추출
    ↓
  국악원 서버에서 WAV 직접 다운로드
    ↓
  AI/data/{조}/{장단}/api_*.wav 저장
```

- 수집 결과: **계면조 160개 + 평조 146개 = 306개 WAV**
- 기존 AIHub 데이터(290개) 대비 더 정제된 악구(phrase) 단위 음원
- `AI/download_phrase_api.py` — 재실행 시 보유 파일 수 체크 후 증분 다운로드

### 2-2. 리듬 모델 재학습 (국악원 API 데이터 기반)

국악원 API의 악구 단위 WAV로 전체 리듬 모델을 재학습했습니다.

**학습 결과 (16/16 — 100%):**

| 장단 | beats | 계면조 WAV | 평조 WAV | 패턴 수 |
|------|:-----:|:---------:|:--------:|:------:|
| 중모리 | 12 | 20 | 19 | 16 |
| 중중모리 | 12 | 20 | 15 | 16 |
| 자진모리 | 12 | 20 | 21 | 16 |
| 굿거리 | 12 | 20 | 25 | 16 |
| 휘모리 | 12 | 20 | 15 | 15 |
| 엇모리 | 6 | 20 | 15 | 16 |
| 세마치 | 9 | 20 | 21 | 16 |
| 진양조 | 6 | 20 | 15 | 16 |

세그먼트 총계: **888개** (재학습 전 649개 → 36% 증가)

### 2-3. 가야금·대금 음고 마르코프 모델 (신규)

**핵심 추가 기능** — 가야금·대금의 멜로디 생성 AI를 구현했습니다.

#### 데이터 수집 (`AI/extract_instruments.py`)

AIHub 국악 데이터셋에 포함된 악기별 WAV를 추출합니다.

| 악기 코드 | 악기명 | AIHub 내 총 파일 수 | 추출 파일 수 |
|-----------|-------|:------------------:|:-----------:|
| PN01, SP01 | 가야금 (산조) | 707개 | 최대 30개/조합 |
| WN01 | 대금 (산조) | 209개 | 최대 30개/조합 |

추출 결과 (468개 WAV, 28개 조합):

```
AI/data_instruments/
├── 가야금/
│   ├── 계면조/ — 굿거리(30) 세마치(22) 엇모리(27) 자진모리(30)
│   │              중모리(30) 중중모리(30) 진양조(30) 휘모리(30)
│   └── 평조/  — 굿거리(25) 세마치(10) 자진모리(10) 중모리(6)
│                  진양조(15) [중중모리·휘모리 데이터 부족으로 제외]
└── 대금/
    ├── 계면조/ — 굿거리(13) 세마치(12) 엇모리(5)  자진모리(29)
    │              중모리(30) 중중모리(9) 진양조(30)
    └── 평조/  — 굿거리(16) 세마치(10) 자진모리(6) 중모리(3)
                  진양조(5)
```

#### 음고 추출 파이프라인 (`AI/pitch_markov.py`)

```
WAV 로드 (sr=22050)
  ↓ librosa.pyin()       프레임별 F0(기본 주파수) 추적
                         fmin=C2(65Hz) ~ fmax=C7(2093Hz)
                         voiced_flag로 무성/잡음 구간 제거
  ↓ onset_detect()       음의 시작점 검출 (같은 hop_length=512 공유)
  ↓ 마디별 지배적 pitch  각 onset 구간 내 최빈(mode) MIDI 음정 → pitch class (0~11)
  ↓ 율명 매핑            0=황 1=대 2=태 3=협 4=고 5=중 6=유 7=임 8=이 9=남 10=무 11=응
→ pitch class 시퀀스 [7, 9, 5, 7, 0, ...]
```

#### 음고 마르코프 학습 (`AI/train_pitch_markov.py`)

```
시퀀스들 집계 → 12×12 전이 횟수 행렬
  → Laplace smoothing (α=1e-6)
  → 행 정규화 → 전이 확률 행렬 (행 합=1)
  → 음고 히스토그램 (12차원) — 시작음 샘플링용
→ AI/models/pitch_{악기}_{조}_{장단}.pkl 저장
```

**학습 완료 모델 (25개):**

| 악기 | 조 | 완료 장단 |
|------|----|-----------|
| 가야금 | 계면조 | 굿거리·세마치·엇모리·자진모리·중모리·중중모리·진양조·휘모리 (8개) |
| 가야금 | 평조 | 굿거리·세마치·자진모리·중모리·진양조 (5개) |
| 대금 | 계면조 | 굿거리·세마치·엇모리·자진모리·중모리·중중모리·진양조 (7개) |
| 대금 | 평조 | 굿거리·세마치·자진모리·중모리·진양조 (5개) |

> 가야금/평조 중중모리·휘모리, 대금/평조 중중모리·엇모리·휘모리·세마치는 AIHub 데이터 부족으로 미학습 (해당 조합 데이터 2개 이하)

---

## 3. 전체 AI 모델 구조 (현재)

### 3-1. 모델 종류

```
AI/models/
├── [리듬 모델 — 16개]  장구 반주 생성용
│   ├── 계면조_중모리.pkl     ← transition_matrix(16×16) + medoids
│   ├── 계면조_자진모리.pkl
│   ├── ... (8 장단 × 2 조)
│   └── 평조_진양조.pkl
│
└── [음고 모델 — 25개]  가야금·대금 멜로디 생성용
    ├── pitch_가야금_계면조_중모리.pkl  ← pitch_transition_matrix(12×12) + note_histogram
    ├── pitch_가야금_계면조_자진모리.pkl
    ├── ... (악기 × 조 × 장단)
    └── pitch_대금_평조_진양조.pkl
```

### 3-2. 모델 내부 구조

**리듬 모델 (.pkl):**
```python
{
  "transition_matrix": ndarray (16×16),  # 패턴 i → j 전이 확률
  "medoids":           ndarray (16×L),   # 각 패턴의 대표 IOI 배열(ms)
  "jo":      str,   "jangdan": str,
  "n_patterns": int, "created_at": str
}
```

**음고 모델 (.pkl):**
```python
{
  "pitch_transition_matrix": ndarray (12×12),  # 음 i → j 전이 확률
  "note_histogram":          ndarray (12,),    # 각 음의 출현 빈도
  "instrument": str,   "jo": str,   "jangdan": str,
  "yul_names":  ["황","대","태","협","고","중","유","임","이","남","무","응"],
  "jo_scale":   {"평조":[0,2,5,7,9], "계면조":[0,3,5,7,10]},
  "created_at": str
}
```

### 3-3. 악기별 AI 생성 방식

| 악기 | 모델 | 생성 원리 |
|------|------|-----------|
| **장구** | 리듬 모델 | IOI 패턴 시퀀스 샘플링 → 세그먼트 WAV 이어붙이기 |
| **가야금** | 음고 모델 | pitch class 시퀀스 샘플링 → 조 스케일 마스킹 → 단음 WAV 조합 |
| **대금** | 음고 모델 | 가야금과 동일 방식, 대금 단음 WAV 사용 |
| **합주** | 리듬 + 음고 | 리듬 모델로 박 위치 결정 → 각 박에 음고 모델로 음 할당 |

---

## 4. 전체 데이터 구조 (현재)

```
AI/
├── data/                     ← 국악원 공공API 훈련 데이터 (306개 WAV)
│   ├── 계면조/
│   │   ├── 중모리/    api_중모리_000.wav ~ 019.wav (20개)
│   │   ├── 자진모리/  20개
│   │   ├── 굿거리/    20개  ···  (8 장단 × 20개 = 160개)
│   └── 평조/
│       ├── 중모리/    19개
│       ├── 자진모리/  21개  ···  (8 장단, 소계 146개)
│
├── data_instruments/         ← AIHub 악기별 분리 데이터 (468개 WAV)
│   ├── 가야금/
│   │   ├── 계면조/ — 8 장단 (22~30개씩)
│   │   └── 평조/  — 6 장단 (6~25개씩)
│   └── 대금/
│       ├── 계면조/ — 7 장단 (5~30개씩)
│       └── 평조/  — 5 장단 (3~16개씩)
│
├── data_aihub/               ← AIHub 초기 추출본 (장단 기준, 290개 — 현재 미사용)
│
├── models/                   ← 학습된 모델 (총 41개 .pkl)
│   ├── {조}_{장단}.pkl                      리듬 모델 16개
│   └── pitch_{악기}_{조}_{장단}.pkl         음고 모델 25개
│
└── segments/                 ← 반주용 마디 오디오 (888개 WAV)
    └── {조}_{장단}/pattern_{ID:02d}/seg_*.wav
```

**데이터 출처 비교:**

| 출처 | 파일 수 | 특징 | 용도 |
|------|:-------:|------|------|
| 국립국악원 공공API | 306개 | 악구 단위, 조·장단 레이블 정확 | 리듬 모델 학습 |
| AIHub 국악 악보 및 음원 | 468개 | 악기별 분리, 산조 연주 10~30초 | 음고 모델 학습 |
| AIHub (초기) | 290개 | 조합 기준 추출, 현재 대체됨 | (미사용) |

---

## 5. 백엔드 API 현황

| 엔드포인트 | 기능 | 상태 |
|-----------|------|------|
| `POST /api/analyze` | 탭 이벤트 → 조·장단·BPM 자동 감지 | ✅ 완료 |
| `POST /api/accompaniment` | 마르코프 반주 생성 (비동기 Job) | ✅ 완료 |
| `GET /api/accompaniment/status/{id}` | Job 상태 조회 | ✅ 완료 |
| `GET /api/accompaniment/available` | 사용 가능 모델 목록 | ✅ 완료 |
| `POST /api/feedback` | Claude API 연주 피드백 | ✅ 완료 |
| `POST /api/tracks/upload` | AAC 녹음 업로드 (10MB 제한) | ⏳ 백엔드 담당자 |
| `POST /api/share` | 공유 링크 생성 | ⏳ 백엔드 담당자 |

---

## 6. 전체 반주·멜로디 생성 흐름

### 장구 반주 생성 (기존)
```
POST /api/accompaniment { jo, jangdan, bpm, bars }
  ↓ 해당 리듬 .pkl 로드
  ↓ 현재 패턴에서 전이행렬 확률 샘플링 (temperature 파라미터)
  ↓ 패턴 ID 시퀀스 → segments/{조}_{장단}/pattern_{ID}/ 에서 WAV 선택
  ↓ BPM 기준 길이 정규화 + 크로스페이드(3%) 이어붙이기
→ 반주 오디오 URL 반환
```

### 가야금·대금 멜로디 AI 생성 (신규)
```
POST /api/melody { instrument, jo, jangdan, n_notes, temperature }
  ↓ pitch_{악기}_{조}_{장단}.pkl 로드
  ↓ note_histogram에서 시작음 샘플링 (조 스케일 마스킹 적용)
  ↓ pitch_transition_matrix로 다음 음 반복 샘플링
     → temperature > 1: 다양성 증가 / < 1: 안정적 흐름
     → 조 스케일 외 음은 확률 0으로 마스킹
  ↓ pitch class 시퀀스 → 단음 WAV 조합
→ 멜로디 노트 이벤트 배열 + 오디오 URL 반환
```

### 합주 AI 생성
```
리듬 모델 → 박 타이밍 결정
  + 음고 모델 → 각 박에 음 할당
→ 장구(리듬) + 가야금(멜로디) + 대금(멜로디) 동기화
```

---

## 7. 기술 스택 (추가분)

| 라이브러리 | 역할 | 추가 시점 |
|-----------|------|-----------|
| `librosa.pyin()` | 멜로디 F0 추적 (voiced/unvoiced 판별) | 2주차 |
| `requests` | 국악원 공공API HTTP 요청 / WAV 다운로드 | 2주차 |
| `xml.etree.ElementTree` | 공공API XML 응답 파싱 | 2주차 |

---

## 8. 핵심 알고리즘 추가 설명

### pyin 기반 음고 추출

```
pyin (Probabilistic YIN):
  - 오디오 프레임마다 기본 주파수(F0) 추정
  - voiced_flag: 유성음 구간만 True (타악기·무음 제외)
  - fmin=C2(65Hz) ~ fmax=C7(2093Hz) 범위에서만 추적
  → Hz → MIDI 변환 → % 12 → pitch class (0~11)

onset 경계별 지배적 pitch class:
  - onset_detect로 각 음의 시작점 검출
  - 한 음의 구간(onset~다음onset) 내 voiced F0 최빈값 사용
  → 비브라토·꺾기 등 국악 시김새로 인한 흔들림 무시, 주음만 추출
```

### 음고 마르코프 생성 + 조 스케일 마스킹

```python
# 평조 5음: [0,2,5,7,9]  계면조 5음: [0,3,5,7,10]
probs = pitch_transition_matrix[current_pitch]

# 해당 조 외 음의 확률 → 0 (재정규화)
for pc in range(12):
    if pc not in jo_scale[jo]:
        probs[pc] = 0.0
probs /= probs.sum()

next_pitch = np.random.choice(12, p=probs)
```

---

## 9. 현재 미완성 항목 및 제약

| 항목 | 내용 | 대응 방안 |
|------|------|-----------|
| 단음 WAV 샘플 | 가야금·대금 pitch class별 단음 WAV 미수집 | 국악원 단음 음원 또는 직접 녹음 필요 |
| 가야금 평조 일부 미학습 | 중중모리·휘모리 — AIHub 데이터 2개 이하 | 추가 데이터 수집 필요 |
| 대금 평조 일부 미학습 | 중중모리·엇모리·휘모리·세마치 — 데이터 부족 | 동일 |
| 조 감지 정확도 | 전조 시 평조/계면조 pitch class 중복 | 프론트에서 조 직접 선택 UI 제공 |
| `/api/melody` 미구현 | 음고 모델 추론 API 엔드포인트 없음 | 3주차 구현 예정 |
| 단음 WAV 조합 로직 | pitch class → 실제 WAV 매핑 서비스 없음 | 단음 샘플 수집 후 구현 |

---

## 10. 3주차 계획

| 우선순위 | 작업 | 담당 |
|---------|------|------|
| 1 | 단음 WAV 수집 (가야금 12현·대금 음정별 녹음 or 국악원 음원) | AI |
| 2 | `/api/melody` 엔드포인트 구현 (pitch 모델 추론) | AI |
| 3 | 합주 생성 로직 (리듬 + 음고 동기화) | AI |
| 4 | DB 연동 — Track 업로드/조회 (`/api/tracks/upload`, `/api/share`) | 백엔드 |
| 5 | 프론트 가야금 UI + 단음 소리 연동 | 프론트 |
| 6 | 전체 통합 테스트 (프론트 ↔ 백엔드 ↔ AI 모델) | 전체 |
